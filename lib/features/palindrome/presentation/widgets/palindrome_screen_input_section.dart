import 'package:flutter/material.dart';

class PalindromeScreenInputSection extends StatelessWidget {
  const PalindromeScreenInputSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
